name: Build & Test Python Wheels

on:
  push:
#  branches: [ master ]
  pull_request:
#  branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

  release:
    types: [published]

# Cancels previous runs of this workflow for the same PR
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' || startsWith(github.ref_name, 'codex/') || github.ref_name == 'wip' }}

jobs:
  build:
    name: Build
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, ubuntu-24.04]
        python-version: ['3.10', '3.11', '3.12', '3.13', '3.14', '3.15-dev']

    steps:
    - uses: actions/checkout@v5

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}

    - name: Define PYTHON_CMD
      run: |
        PYTHON_VER="`echo ${{ matrix.python-version }} | sed 's|-dev$||'`"
        echo "PYTHON_CMD=python${PYTHON_VER}" >> $GITHUB_ENV

    - name: Install dependencies
      run: |
        ${PYTHON_CMD} -m pip install --upgrade pip
        pip install --upgrade setuptools wheel
        pip install -r requirements.txt

    - name: Build, Install & Test
      run: |
        ${PYTHON_CMD} setup.py build
        ${PYTHON_CMD} setup.py sdist
        sudo "`which ${PYTHON_CMD}`" setup.py install
        pip install dist/sippy*.gz
        ${PYTHON_CMD} -m unittest

  cache_qemu_image:
    name: Cache QEMU Image
    uses: sippy/cimagic/.github/workflows/GetCachedContainer.yml@v3
    with:
      image: tonistiigi/binfmt:latest
      platforms: host
      cache-key-prefix: qemu-binfmt-image
      cache-name: qemu-binfmt-image

  cache_buildkit_image:
    name: Cache BuildKit Image
    uses: sippy/cimagic/.github/workflows/GetCachedContainer.yml@v3
    with:
      image: moby/buildkit:buildx-stable-1
      platforms: host
      cache-key-prefix: buildkit-image
      cache-name: buildkit-image

  voiptests:
    name: VoIPTests
    needs:
    - cache_qemu_image
    - cache_buildkit_image
    uses: ./.github/workflows/.voiptests.yml
    with:
      rtpp-image-tag: latest-${{ matrix.rtpp-image-tag }}
      qemu-image-cache-key: ${{ needs.cache_qemu_image.outputs.image-cache-key }}
      qemu-image-cache-path: ${{ needs.cache_qemu_image.outputs.image-cache-path }}
      qemu-image-cache-tar: ${{ needs.cache_qemu_image.outputs.image-cache-tar }}
      buildkit-image-cache-key: ${{ needs.cache_buildkit_image.outputs.image-cache-key }}
      buildkit-image-cache-path: ${{ needs.cache_buildkit_image.outputs.image-cache-path }}
      buildkit-image-cache-tar: ${{ needs.cache_buildkit_image.outputs.image-cache-tar }}
    strategy:
      fail-fast: false
      matrix:
        rtpp-image-tag: ['debian_13-slim', 'ubuntu_latest']

  publish_wheels:
    name: Publish Puthon Wheels
    needs: [build, voiptests]
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/sippy
    permissions:
      id-token: write
    steps:
    - uses: actions/checkout@v5

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.14'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install --upgrade setuptools wheel
        pip install -r requirements.txt

    - name: build
      run: python setup.py sdist

    - name: Publish package distributions to PyPI
      if: github.event_name == 'release' && github.event.action == 'published'
      uses: pypa/gh-action-pypi-publish@release/v1

  roll_release:
    name: Release
    needs: [publish_wheels]
    permissions:
      contents: write
    uses: sippy/cimagic/.github/workflows/RollReleaseDraft.yml@v3
